
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = waybar
exec-once = desktop_portal
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = swww-daemon
exec-once = /usr/bin/lxpolkit
exec = sleep 0.5s && swww img --transition-type wipe --transition-angle 30 --transition-step 90  /home/casan/Pictures/wallpapers/wallpaper01.png
# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf
source = ~/.config/hypr/mocha.conf

# Set programs that you use
$terminal = kitty
$fileManager = nemo
$menu = wofi --show drun
$browser = firefox -profile /home/casan/.local/share/firefox/Cool_User
$test_browser = firefox -P Pentest01
$test_browser02 = firefox -P TestSurf
$browser2 = librewolf
$power = wlogout -p layer-shell &
$aud_switch = /home/casan/.local/bin/aud_switch

# Some default env vars.
env = _JAVA_AWT_WM_NONREPARENTING,1
env = XCURSOR_SIZE,24
env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORM=wayland;xcb
env = QT_QPA_PLATFORMTHEME=qt6ct
env = QT_AUTO_SCREEN_SCALE_FACTOR=1
env = QT_WAYLAND_DISABLE_WINDOWDECORATION=1

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 4
    gaps_out = 10
    border_size = 2
    col.active_border = $blue $mauve 90deg
    col.inactive_border = rgba(595959aa)

    layout = master

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 2
    
    blur {
        enabled = true
        size = 3
        passes = 1
    }

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = 0 # Set to 0 to disable the anime mascot wallpapers
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$

windowrule = center, title:^(Java)$
windowrule = noborder, title:^(Java)$
windowrule = noborder, ^(burp-StartBurp)$
windowrule = noborder, ^(Gimp-2.10)$
windowrule = float, ^(Gimp-2.10)$

windowrulev2 = workspace 4, class:^(Spotify)$
windowrulev2 = workspace 2, class:^(firefox)$
windowrulev2 = workspace 7, class:^(Vmware)$
windowrulev2 = workspace 3, class:^(burp-StartBurp)$
windowrulev2 = workspace 3, title:^(Java)$
windowrulev2 = workspace 5, class:^(Gimp-2.10)$

# layer rules
# apply blur on notification layer such as mako or dunst
layerrule = blur, notifications 
# skip the blur on completely transparent parts (such as mako's margin)
layerrule = ignorezero, notifications


# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
windowrulev2 = nomaximizerequest, class:.* # You'll probably like this.


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# binds
binde =, XF86AudioRaiseVolume, exec, /home/casan/.local/bin/audioscpt 1
binde =, XF86AudioLowerVolume, exec, /home/casan/.local/bin/audioscpt 2

# Some Useful Binds
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive, 
bind = $mainMod SHIFT, Q, exec, $power,
bind = $mainMod, M, exit, 
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating, 
bind = $mainMod SHIFT, m, exec, /home/casan/.local/bin/audioscpt 3
bind = $mainMod, w, exec, $browser2
bind = $mainMod SHIFT, w, exec, $browser
bind = $mainMod SHIFT, t, exec, $test_browser02
bind = $mainMod SHIFT, a, exec, $aud_switch
bind = $mainMod SHIFT, e, exec, dunstctl history-pop
bind = $mainMod SHIFT, n, exec, dunstctl close-all
bind = $mainMod, T, exec, [workspace special:magic] $test_browser
bind = $mainMod, n, exec, dunstctl close
bind = $mainMod, Y, exec, rofi -show emoji -emoji-format "{emoji}" -modi emoji -theme emoji
bind = $mainMod, C, exec, rofi -show calc -modi calc -no-show-match -no-sort -theme calc
bindr = $mainMod, p, exec, pkill rofi || rofi -show drun

# layout keys
bind = $mainMod SHIFT, RETURN, layoutmsg,swapwithmaster master # swapping with master

# Windowed fullscreen with mainMod + P
bind = $mainMod, F, fullscreen, 0
# Windowed fakefullscreen with mainMod + SHIFT + P
bind = $mainMod SHIFT, F, fakefullscreen

# Move focus with mainMod + arrow keys
bind = $mainMod, l, movefocus, l
bind = $mainMod, h, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
